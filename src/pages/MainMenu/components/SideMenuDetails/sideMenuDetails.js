import React, { useState } from "react";
import { Text, View, Image, TouchableOpacity } from "react-native";

import styles from "./styles";

export default function SideMenuDetail(product){

    return(
        <View>
            <Text>side menu detail</Text>
        </View>
    );

    

}