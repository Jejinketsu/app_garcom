import React from "react";
import { Text, View } from "react-native";

export default function SideMenuCart(props){

    return(
        <View>
            <Text>side menu cart</Text>
        </View>
    );

}